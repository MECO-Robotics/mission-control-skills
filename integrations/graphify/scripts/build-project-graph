#!/usr/bin/env node
const cli = require("../src/cli");
const result = cli.commandBuildProjectGraph(process.argv.slice(2));
console.log(`graph_path=${result.graphPath}`);
console.log(`external=${result.external}`);
