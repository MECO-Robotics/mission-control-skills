#!/usr/bin/env node
const cli = require("../src/cli");

const result = cli.commandBuildTaskGraph(process.argv.slice(2));
if (!result) {
  process.exit(1);
}
console.log(`graph_path=${result.graphPath}`);
if (result.summaryPath) {
  console.log(`summary=${result.summaryPath}`);
}
