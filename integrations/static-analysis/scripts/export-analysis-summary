#!/usr/bin/env node
const cli = require("../src/cli");
const result = cli.commandExportAnalysisSummary(process.argv.slice(2));
console.log(`output=${result.outputPath}`);
console.log(`findings=${result.metrics.total}`);
