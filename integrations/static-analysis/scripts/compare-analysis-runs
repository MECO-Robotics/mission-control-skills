#!/usr/bin/env node
const cli = require("../src/cli");
const result = cli.commandCompareRuns(process.argv.slice(2));
console.log(`output=${result.outputPath}`);
console.log(`new=${result.delta.new.length}`);
console.log(`resolved=${result.delta.resolved.length}`);
