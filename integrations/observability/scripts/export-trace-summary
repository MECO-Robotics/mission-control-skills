#!/usr/bin/env node
const cli = require("../src/cli");
const result = cli.commandExportTraceSummary(process.argv.slice(2));
console.log(`output=${result.output_path}`);
console.log(`total=${result.total_traces}`);
