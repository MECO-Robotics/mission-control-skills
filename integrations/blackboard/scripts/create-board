#!/usr/bin/env node
const cli = require("../src/cli");

const result = cli.commandCreateBoard(process.argv.slice(2));
console.log(`board_id=${result.board.board_id}`);
console.log(`created=${result.created}`);
console.log(`board_path=${result.boardPath}`);
