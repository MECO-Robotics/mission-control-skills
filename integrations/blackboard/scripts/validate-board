#!/usr/bin/env node
const cli = require("../src/cli");

const result = cli.commandValidateBoard(process.argv.slice(2));
console.log(`valid=${result.valid}`);
console.log(`failures=${result.failures.length}`);
for (const row of result.failures) {
  console.log(`- ${row}`);
}
