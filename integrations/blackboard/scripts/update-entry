#!/usr/bin/env node
const cli = require("../src/cli");

const result = cli.commandUpdateEntry(process.argv.slice(2));
console.log(`entry_id=${result.entry.id}`);
console.log(`board_id=${result.board.board_id}`);
console.log(`status=${result.entry.status}`);
console.log(`updated_at=${result.entry.updated_at}`);
