#!/usr/bin/env node
const engine = require("../src/engine");
const result = engine.evaluateTaskContext(process.argv.slice(2));
console.log(`task_score=${result.score}`);
console.log(`pass=${result.pass}`);